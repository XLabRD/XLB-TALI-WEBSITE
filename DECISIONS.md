# Decisions — Tali

The living ledger of choices behind this site. Created by the `content-site`
skill. **OPEN** rows are questions the author deferred — each one has a
default currently in effect and `TODO(DEC-n)` markers at the code sites
(`grep -rn "TODO(DEC-" src/ public/ *.mjs` to find them). To resolve or change
any row, ask Claude to run `/content-site` in this project.

Statuses: **OPEN** (deferred, default in effect) · **DECIDED** ·
**REVISED** (decided, later changed — history kept in Notes).

| ID | Question | Status | Current answer / default in effect | Where it lives | Notes |
| --- | --- | --- | --- | --- | --- |
| DEC-1 | Name & purpose? | DECIDED | Tali — premium IoT guardian (temp/humidity/vibration/light) for wine, cigars & delicate collections; e-paper display + Puk sensors + app | Copy throughout `src/content/` | 2026-07-01: interview |
| DEC-2 | Site type? | DECIDED | Product site — one long page per language | `src/pages/` | 2026-07-01: interview |
| DEC-3 | Copy language? | DECIDED | Bilingual en-US + es-MX, side-by-side fields in every content entry | `src/content/**` (`en`/`es` keys) | 2026-07-01: interview |
| DEC-4 | Which locale at the root URL? | DECIDED | English at `/`, Spanish at `/es/`. Auto-redirect on `/` is **location-first**: a remembered switcher click wins; else a Mexican timezone → `/es/`; else **no timezone at all → `/es/`**; only a located, non-Mexican visitor with an English browser stays on English. Detection results are never persisted — only a switcher click is, under `tali-locale` | `src/layouts/Base.astro` (`localeRedirect` + `set:html`), `src/components/Header.astro` (choice memory) | 2026-07-28: author chose language-based detection defaulting to Spanish when unsure; root stays English for crawlers/canonical. **2026-09-01: the detector had never run.** It was authored as `<script is:inline>{`…`}</script>`, but Astro treats script contents as raw text, so the `{`…`}` wrapper was emitted verbatim — the browser parsed a block containing a template literal, evaluated the string and discarded it. `/` served English to everyone for six weeks, and no error was logged because inert code throws nothing. Now injected via `set:html` from a frontmatter const. Same pass revised the signal: language ≠ location, and this audience is Mexican on English-configured machines, so timezone (the only location signal a static page gets — no network, no consent prompt, runs in `<head>` so no flash) outranks `navigator.languages`. Also **rotated the storage key**: the old `tali-lang` stored *guesses* under the same key as clicks, so one English-browser visit pinned a visitor forever and would have outlived this fix; the detector now deletes `tali-lang` on sight and reads `tali-locale`, which only the switcher writes |
| DEC-5 | Domain? | DECIDED | `https://tali.my` | `astro.config.mjs` `site`, `public/CNAME`, `public/robots.txt` | 2026-07-01: interview |
| DEC-6 | Sections? | REVISED | Hero · System · Exploded · App · Features · Film · Founder Edition · Ship schedule · FAQ · Contact | `src/components/Landing.astro` | 2026-07-01: was Hero·System·Features·Editions·FAQ·Contact; shortened per author. FAQ content kept unrendered in `src/content/faq/` + `src/components/Faq.astro`. 2026-07-28: added "When will it ship?" batch table (`ShipSchedule.astro`, `home.shipBatches`) after the edition card per author. 2026-07-28: FAQ re-enabled below it (7 entries incl. new Tali Pro renewal/grace-period answer); FAQ nav link restored |
| DEC-7 | Who edits content? | DECIDED | Keystatic local mode (`npm run dev` → `/keystatic`) | `keystatic.config.ts`, `astro.config.mjs` | 2026-07-01: interview |
| DEC-8 | Brand assets (logo, colors, fonts)? | REVISED | Real logo wordmark + Ô-mark favicon integrated from `~/Desktop/Assets/logo.png`; canvas light & warm; copper accent and Fraunces/Inter/IBM Plex Mono remain my picks | `public/images/tali-logo.png`, `public/favicon.png`, `src/styles/global.css` `:root` | 2026-07-01: dark→light per author. 2026-07-02: logo + favicon from author's assets. 2026-07-02: accent copper→verdigris teal (#2e7573), matched to the app UI and darkened for contrast, per author; fonts still my picks — see TODO(DEC-8) in global.css |
| DEC-9 | Astro major version? | DECIDED | Astro 6.4.x (not 7) — Keystatic 5.1 peer-depends on astro ≤6 | `package.json` | 2026-07-01: revisit when Keystatic supports Astro 7 |
| DEC-10 | Contact form fields? | DECIDED | Name, email, message (+ honeypot) | `src/components/Contact.astro` | Default accepted |
| DEC-11 | Form provider endpoint & notification inbox? | OPEN | `formEndpoint` empty → form falls back to `mailto:hello@tali.my` (unverified placeholder inbox) | `src/content/site/settings.json`, `src/components/Contact.astro`, `src/content.config.ts` | 2026-07-25: form now lives in a modal dialog; Web3Forms chosen (paste bare access key → enables built-in hCaptcha). Create the key with the real inbox, paste in Keystatic |
| DEC-12 | Newsletter signup? | OPEN | None built | — | Add via `/content-site` if wanted |
| DEC-13 | Deploy target? | DECIDED | GitHub Pages via Actions | `.github/workflows/deploy.yml` | 2026-07-01: interview |
| DEC-14 | Git remote? | DECIDED | `git@github.com:XLabRD/XLB-TALI-WEBSITE.git` (X-Lab org) | `.github/workflows/deploy.yml` | 2026-07-23: initial push to main. Pending owner actions: Pages Source = GitHub Actions, custom domain tali.my + Enforce HTTPS, DNS A records (185.199.108–111.153), org domain verification |
| DEC-15 | Analytics? | OPEN | None (fastest, no consent banner) | — | If wanted: Plausible/Fathom/GoatCounter over GA4 |
| DEC-16 | Social links? | OPEN | Empty values → links hidden in footer | `src/content/site/settings.json`, `src/components/Footer.astro` | |
| DEC-17 | Pricing & editions? | DECIDED | All USD, IVA (16%) + MX shipping included: **Founders Limited Edition $185** (pre-order open, ships first Aug–Sep 26, marked, 24 mo Cloud Pro, only 50) · **Signature Edition $185** (not for sale, est. Q1 27, 12 mo) · **Standard $198** (est. Q2–Q3 27, 6 mo) · extra Puk $55 · Cloud Pro $39/yr or $99 lifetime (≤5 Talis). Same hardware Founders/Signature (anodized machined aluminium); differences stated wherever both appear. Founders card shows Stripe's live price; all other figures hardcoded literals | `workers/checkout/`, `src/components/`, `src/content/`, Stripe product | 2026-07-02: pitch-deck MXN targets superseded. 2026-07-26–28: $148 → $155 → $185; "Limited Edition" tier renamed **Signature Edition** (2026-07-28 spec); savings FAQ removed (same-price rule); waitlist deferred (none exists yet). Test-mode Stripe — live price object needed at launch. 2026-08-18: USD stays the base currency but the Founder price gains an **MXN `currency_option` ($2,199 MXN)** — Mexican-issued cards decline foreign-currency auths (`currency_not_supported`) and OXXO is MXN-only, so a USD-only price both lost sales and hid OXXO. Checkout picks the option by buyer location. **The site still quotes USD in both locales** — the peso amount is a checkout-time convenience only, so the displayed price stays one figure everywhere. 2026-08-18 (later): **edition structure revised** — Founders is **100** units, not 50 (see DEC-27); Signature is no longer a separate-price tier but the unlimited default series at the **same $125**, opening when Founders sells out; Standard takes no pre-orders. Ship windows superseded: Founders Sep–Dec 26, Signature Feb–Apr 27. **2026-08-26: pre-order framing dropped** — the word "pre-order" is gone from the site by author preference; every CTA and body string moved to buy/available-now language in both locales. **Ship windows are unchanged** (Wave 1 still Sep 26) (`heroCtaPrimary`, `navPreorder`, plan `cta`, `pricingNote`, Founders/Standard `shipBatches[].window`, the how-to-buy + Signature FAQs, and the Resend welcome preheader). Internal identifiers stay unchanged (`data-preorder`, the `preorder` FAQ flag, `navPreorder`, `preorder-signature.json`) |
| DEC-20 | Founder Edition bundle details (Puk count, numbering, perks)? | OPEN | Placeholder: 1 display + 2 Puks, individually numbered, lifetime priority support | `src/content/plans/founder.json` | Contents are my invention — confirm or edit in Keystatic |
| DEC-18 | App store links? | OPEN | App section with 3 real screenshots, no store badges/links yet | `src/components/AppShowcase.astro`, `public/images/app-*.jpg` | Add badges when the app ships publicly. 2026-07-02: screenshots (IMG_8292–8294) integrated; IMG_8295/8296 (settings, alerts) unused. 2026-08-06: `/hello` now points at the TestFlight beta (`https://testflight.apple.com/join/8BrjthWQ`) and its badge reads "Download Beta on the App Store"; the main-site AppShowcase still has no store badge — swap `settings.appStoreUrl` for the public App Store link at launch and the badge wording reverts automatically. 2026-09-03: Android shipped — `settings.playStoreUrl` (`https://play.google.com/store/apps/details?id=com.xlab.tali`) adds a Google Play badge beside the App Store one on `/hello`, and Android visitors are redirected to it the way iOS visitors are; unknown browsers see both badges |
| DEC-19 | Product photography? | REVISED | Real prototype photo (rotated/cropped from `IMG_6381.jpeg`) in the hero | `public/images/tali-prototype.jpg`, `src/components/Hero.astro` | 2026-07-02: replaced CSS mock with author's photo. `DeviceMock.astro` kept unused as fallback. 7 product videos (.MOV) exist in `~/Desktop/Assets` — unpublished, would need transcoding for web |
| DEC-21 | Order/checkout service? | REVISED | Embedded Stripe Checkout in a modal on the pricing section, sessions minted by a Cloudflare Worker (`workers/checkout/`); return pages `/thanks/` + `/es/thanks/` handle paid / OXXO-voucher-pending / incomplete. Until `checkoutEndpoint` + `stripePublishableKey` are set in Keystatic, CTA falls back to the Payment Link (`orderUrl`), then `#contact` | `workers/checkout/`, `src/components/Pricing.astro`, `src/components/ThankYou.astro`, `src/content/site/settings.json`, `src/content.config.ts`, `keystatic.config.ts` | 2026-07-23: chose Stripe Payment Link (MXN/OXXO, static-friendly) over Shopify/Snipcart/Gumroad; Lemon Squeezy/Paddle ruled out (digital-only MoR). 2026-07-26: upgraded to embedded checkout (buyer stays on tali.my); worker keeps the public build static. Pending owner actions — see `workers/checkout/README.md`: deploy worker, set `STRIPE_PRICE_ID` + `STRIPE_SECRET_KEY`, paste endpoint + publishable key in Keystatic. 2026-07-30: **live mode switched on** — launch price $185 USD tax-inclusive (`price_1TypWV2QQj9hlFmfJYXOVDNE`, product default; stray $148/$155 live prices archived), live Payment Link rebuilt as fallback, worker redeployed with live secret + webhook secret (`we_1TypL3…`, 5 events), `pk_live_…` in settings. Verified: live session mints OK. Remaining: one real-card end-to-end order + refund. 2026-08-18: multi-currency price (see DEC-17) is a **Stripe-side change only** — no worker or site code changes, since `createSession` already omits `currency` (letting Stripe localize by buyer location) and the site quotes USD in both locales. Owner action: `currency_options[mxn]` on the price + OXXO enabled in payment-method settings |
| DEC-23 | Device-QR onboarding page | DECIDED | Standalone `/hello` page (unlinked, noindex, sitemap-excluded): Hello! + app download + Get help mailto | `src/pages/hello.astro`, `astro.config.mjs`, `settings.appStoreUrl` | 2026-07-24: QR on device screens points to https://tali.my/hello. iOS-only for now. English-only — add locale detection if needed. 2026-08-06: `appStoreUrl` set to the TestFlight beta, so iOS visitors are now auto-redirected there |
| DEC-24 | Order-status updates for customers? | DECIDED | Fulfillment status lives in Stripe payment **metadata** (`order_status`, `tracking_url`), edited by hand in the dashboard; the buyer's `/thanks/?session_id=…` link doubles as a live status page (received/shipped + tracking). Notifications are manual email at founder volume | `workers/checkout/worker.js` (`/session-status`), `src/components/ThankYou.astro`, `workers/checkout/README.md` | 2026-07-26: chose dashboard-metadata over building an admin; automated status emails (Stripe webhook → worker → Resend) deferred — revisit if volume grows |
| DEC-25 | Order ops: Notion + automated emails | DECIDED | Paid checkout → row in Notion "Pedidos" DB + Resend welcome email; staff flip Status in Notion (⚡ automation → `/notion-webhook`, KV-deduped) → shipped/canceled email; Stripe refund/dispute → Status auto-flips to Canceled → cancel email. Emails from `orders@tali.my` (Resend, domain verified), reply-to `hello@tali.my` | `workers/checkout/` (`worker.js`, `notion.js`, `emails.js`, README) | 2026-07-27: architecture per author — Notion is the ops console; welcome email sent directly from the Stripe webhook (not via Notion). Verified end-to-end same day (all three emails delivered). Same day: `/session-status` re-pointed at Notion, metadata seeding dropped — Notion is sole source of truth. Abandoned-cart capture added (`checkout.session.expired` → Status `Abandoned`, no email); declined-payment capture considered and rejected (same-PI retries would duplicate/clutter rows) |
| DEC-28 | One-command price changes | DECIDED | `npm run set-price -- <usd> [--apply]` (`scripts/set-price.mjs`) is the only supported way to change the Founder price: creates a new Stripe Price **with** its MXN `currency_options` and `metadata[fx_sync]`, archives the old, rewrites `STRIPE_PRICE_ID`, and rewrites every hardcoded `$` literal across `src/content/` in both locales. Dry-run by default; `--apply` commits nothing, so the content diff is reviewed by hand | `scripts/set-price.mjs`, `package.json`, `workers/checkout/fx.js` (`fetchRate` exported), README | 2026-08-18: written after finding a price change has 11 steps and two silent failure modes. Stripe Prices are immutable on `unit_amount` (update takes only active/currency_options/lookup_key/metadata/nickname/tax_behavior), so a change is always new-price-plus-archive; a fresh price has no peso option until the 16:30 UTC cron, and that window reintroduces the DEC-17 card decline — hence setting it at creation. Peso figure comes from `fx.js` so script and cron round identically and the next run is a no-op. Known gap: the Payment Link (`orderUrl`) can't be rebuilt automatically and is printed as a manual step |
| DEC-27 | Founders waves, cap + Signature handoff | DECIDED | Founders is a hard 100-unit run in two waves of 50 — **Wave 1 ships Sep 26, Wave 2 Oct–Dec 26**; unit 101+ is the **Signature Series** (unlimited, Feb–Apr 27, same $125). Standard takes no pre-orders. Pricing card shows the wave for the *next* unit (worker `/inventory`); the buyer's own wave is stamped at purchase into Notion (`Position`, `Wave`) and never recomputed, then echoed on `/thanks/`. **No totals are ever exposed** — `remaining` is withheld server-side until a wave is within `WAVE_COUNTDOWN_AT` (15) | `workers/checkout/inventory.js`, `notion.js`, `worker.js`, `wrangler.toml`; `src/components/Pricing.astro`, `ThankYou.astro`; `home.json`/`ui.json` + both schemas | 2026-08-18: waves ≠ editions — `shipBatches` stays the edition table, waves are a second axis inside Founders. Count comes from Notion rows with Status `Received`/`Shipped` (Canceled/Abandoned release the unit), KV-cached 60s, busted by the webhook; a running counter was rejected because it drifts on webhook retries and manual fixes. Cap enforced in `createSession`, not just the button — but **fails open** on a Notion outage (losing a sale beats a refundable oversell). Known gap: the count is a snapshot, so simultaneous buyers at unit 100 can both pass — acceptable at this volume, reconcile by hand. **Open:** Payment Link (`orderUrl`) bypasses the worker entirely and needs a Stripe `restrictions` completed-sessions limit; Signature Product/price not yet created (`SIGNATURE_PRICE_ID` unset) and will need `metadata[fx_sync]` + the DEC-26 cron generalized, or Signature buyers hit the DEC-17 card decline |
| DEC-26 | Keeping the MXN price in step with USD | DECIDED | Daily Cloudflare cron (16:30 UTC) in the checkout worker recomputes the price's MXN `currency_option` from a live USD/MXN rate + a **3% buffer**, rounded up to x49/x99. Guards fail closed: rate must land in 10–30, single-day move capped at 8%, floor of $500 MXN, and the write is skipped when nothing changed. Staff emailed only on failure or a blocked move | `workers/checkout/fx.js`, `worker.js` (`scheduled`), `emails.js` (`sendAlertEmail`), `wrangler.toml` (`[triggers]`, `FX_BUFFER_PCT`) | 2026-08-18: Stripe **cannot** do this conversion for us — Adaptive Pricing requires the price currency to be a settlement currency, and Mexico has no multi-currency settlement (MXN is the account's only one), so a USD base price is ineligible. Flipping the base to MXN would qualify but forces peso display and loses OXXO (Adaptive Pricing doesn't support it). Hence a manual `currency_options` entry, kept fresh by cron. Effective buffer is 3–5% once rounding is counted; 50-peso granularity means ordinary daily wiggle produces no write at all |
| DEC-22 | Scroll-driven 3D exploded view | DECIDED | Pre-rendered 80-frame WebP scrub (Blender/EEVEE from `~/Desktop/Assets/3D/Tali v13.fbx`) on a canvas; no runtime 3D | `src/components/ExplodedView.astro`, `public/images/explode/tali/`, scratchpad `explode_render.py` + `draw_ui.py` | 2026-07-23: image sequence chosen over Three.js (643k-tri model, zero JS deps, reliable). Reduced-motion/no-JS fall back to static exploded still. Puk model pending — rerun pipeline when its FBX arrives. Render scripts in `scripts/3d/`. 7 feature callouts (HTML overlay, labels in `home.explodeCallouts`, anchors in component) fade in at full explosion; anchor coords are tied to frame_079 — re-check after any re-render |
| DEC-29 | Traffic figures: how many visits, how many Buy-now presses? | DECIDED | Counted by the **checkout worker itself**, not a third-party script — the site already calls `/inventory` on every page load and `/create-checkout-session` on every Buy-now press, so both routes bump a KV counter as a side effect (no beacon, no cookies, no consent banner, nothing added to the public bundle). Read back by **`/stats/`** on the site: standalone page like `/hello` (noindex, sitemap-excluded, English-only), unlocked by a `STATS_KEY` secret that the page saves to `localStorage` and strips from the URL. Shows lifetime views · Buy-now presses · orders, the view→press rate, and a bar chart per metric over 7/14/30/90 days | `workers/checkout/stats.js`, `worker.js`, `wrangler.toml`, `README.md`; `src/pages/stats.astro`; `astro.config.mjs` sitemap filter | 2026-08-26: rejected paid analytics (Plausible/Fathom ~$9–14/mo) and Cloudflare Web Analytics — the free beacon can't count a button press, which is half the question. **Views are a floor, not a census**: only loads that ran JS register (free bot filtering) and `/inventory`'s 30s browser cache folds reload bursts into one. A press is counted *before* the cap check, so a refused one still shows the intent. Orders come from Notion, not from here, so the funnel's last stage can't disagree with the cap — but it is all-time, so it may predate counting and must not be read as a conversion of the views shown. **Counting is append-only, never incremental**: KV has no compare-and-swap, so a read-add-write counter drops concurrent hits — measured on the deployed worker, **ten simultaneous requests recorded two**, while sequential traffic counted fine, which is what makes it a trap (correct in testing, collapses during a spike). Each event appends its own `stats:e:<metric>:<date>:<uuid>` key (TTL 8d) and counting is `list()` over the day's prefix; the existing daily cron folds closed days into a month map (assigned not added, so re-runs are idempotent) and advances a lifetime key, while today is always counted live so the page is never a day behind. Cost: one KV write per event, so on the Workers **free plan** the 1,000/day write quota is also the counting ceiling — it stops silently, so check the plan before trusting a big day. Reads are KV-cached up to 60s, so a refresh can lag a minute. **Nothing is retroactive**: counting starts the day the worker is deployed. Only requests with an allowed `Origin` count |
| DEC-30 | Tracking for local same-day deliveries with no tracking number? | DECIDED | A **`/track/` + `/es/track/`** page (noindex, sitemap-excluded, public) that explains a local courier is delivering today and that the absence of a tracking number is normal, not a lost parcel. Reached with **no pipeline change**: staff paste its URL into the order row's `Tracking URL` in Notion, which already drives the "Track your shipment" button on `/thanks/` **and** in the shipped email (`emails.js`). Works bare (`/track/`) or personalized (`/track/?session_id=…`, the Session ID being on the same row) showing order number, unit and delivery address | `src/components/Track.astro`, `src/pages/track.astro` + `es/track.astro`; `ui.json` + both schemas; `astro.config.mjs` sitemap filter; `worker.js` `/session-status` | 2026-08-26: author's mechanism — reuse the `Tracking URL` field rather than build a parallel channel; an empty field hides the button in both places, so a link that says "there is no number" beats no link. Both URL forms accepted deliberately: a forgotten session ID degrades to the generic message instead of a broken link in a buyer's inbox. `/session-status` gained `order_number` + `address` — same trust boundary as the `customer_email` it already returned, but a **forwarded link now exposes a shipping address**, so send the bare URL when that matters. `Shipped` is the default state (the link only exists once dispatched) and also the fallback when the worker is unreachable. **No Delivered state** (author's scope): it would need a new Notion select option, so the page instead invites the buyer to write in if nothing arrives by end of day. Driver name/phone and an ETA window were offered and declined. 2026-08-26 (later): author's copy pass — the shipped state opens with a truck icon (inline SVG, `--accent`) and **"GREAT NEWS!"**, and the delivery promise ("hand delivering this order to you today **before 6pm**") closes the page rather than leading it, paired with the no-tracking-number note. Preparing/Canceled gained their own titles so a canceled order no longer reads "Out for delivery". `trackLead` renamed `trackDelivery` to match where it renders. The 6pm window is fixed copy in `ui.json`, not a per-order field. 2026-08-27: layout settled — headline **GREAT NEWS!**, sub-headline **"Your order is out for delivery"**, the 6pm promise beneath it, then the order details, then why there is no tracking number ("same-day, special, ultra VIP hand delivery"), then the help line, now also covering general questions. `trackSubtitle` added; `trackDelivery` carries only the 6pm window and moved back above the details. 2026-08-27 (later): **`Tracking URL` is now auto-filled at order creation** (`createOrderPage`) with the locale-correct `/track/?session_id=…`, so no row is ever missing a link and there is nothing to paste. Safe because the page already reads "preparing your order" for a `Received` row — the link is simply correct early. Written **only at creation**, so replacing it with a real carrier link before flipping to `Shipped` sticks; abandoned rows stay empty. Side effect accepted by the author: `/thanks/` now shows its "Track your shipment" button from the moment payment lands. The welcome email is unaffected — it only ever linked the status page. 2026-08-27 (fix): pre-filling the URL invalidated the page's own default. Out-for-delivery was the markup default because "the link only exists once dispatched" — untrue the moment the URL is written at order creation, so an unresolved id claimed a delivery that was not happening (reported on a `Received` order). Now: a link **with** an id starts on the conservative preparing message and is promoted only when the lookup says `shipped`; a worker outage or dead id stays conservative. The bare link keeps the out-for-delivery default, which is what staff paste at dispatch. The id key is also matched loosely (`sessionid`, `sessionId`, `session-id`, any case) — a mistyped key used to look up nothing and silently show the optimistic message |
| DEC-31 | Conversion pass: guarantee, availability, proof | DECIDED | **30-day money-back guarantee** — stated on the pricing note, as a linked bullet on the Founders card, and as its own FAQ (`money-back`, slot 10). **Wave 1 reframed** from a ship window to "Available now — 100 units, first come, first served" (editions table + the live wave line + the post-purchase `thanksWave1` line); **Wave 2 and Signature keep their windows**. **Unsubstantiated social proof removed**: the hero trust line is now empty (Hero renders it only when set) and the film caption no longer claims sommeliers trust Tali. **"No subscription required" surfaced** from the FAQ onto the pricing card, linked to `#faq-need-subscription` | `src/content/site/home.json`, `ui.json`, `plans/founder.json`, `faq/money-back.json` | 2026-08-27: prompted by a marketing read of the live site. **The funnel figures first used were misleading** — `/stats` had only been counting for two days (48 views), which was read as a traffic problem; the author corrected it: the campaign drove **400+ visits for 2 orders, ~0.5% conversion**, so the constraint is the funnel, not arrivals. Buy-click counts for that period were never recorded, but Stripe holds them — every Buy press mints a Checkout Session. Rationale: an unknown brand asking $135 to guard a far more valuable collection had **no risk reversal anywhere** and no returns/warranty FAQ among 18; "Available now" sat beside "ships Sep – Dec 26", which reads as bait-and-switch; and "the best and most demanding sommeliers trust Tali" carried no name, face or quote, so it cost credibility rather than adding it. **Open:** return-shipping terms unspecified in the FAQ — decide who pays. Quantifying included Cloud Pro was considered and **rejected**: at $8/yr the 24 months reads as $16 on a $135 order and makes the offer look smaller. `explodeCaption` ("Designed with sommeliers, cigar specialists and collectors") was **left in place** — a development claim, not a trust claim. 2026-08-27 (later): fine print under the buy button — "* Shipping and taxes within Mexico included" / "* Envío e impuestos en México incluidos" — answering what the price covers while the buyer is deciding, not at checkout. Uses the `priceNote` field, which **existed in both schemas and was never rendered**, and held one shared Spanish string for both locales; it is now per-locale inside `en`/`es` |
| DEC-32 | What currency does the Spanish site quote? | DECIDED | **`/es/` quotes MXN, `/` keeps USD** — superseding DEC-17's one-figure rule. The peso figure is **never committed to content**: `/price` now expands `currency_options` and returns `{unit_amount, currency, mxn}`, and the Spanish page renders `mxn` at runtime. That is Stripe's own currency_option — the exact amount checkout charges a Mexican card — kept current by the daily FX cron (DEC-26). Swapped in two places: the Founders card price and the editions-table rows, and a row swaps **only if its USD amount equals the Founder price**, so an edition priced differently later is never relabelled. Spanish prose no longer names a figure at all | `workers/checkout/worker.js` (`price()`), `src/components/Pricing.astro`, `ShipSchedule.astro`, `home.json` + `faq/founders-vs-signature.json` (es) | 2026-08-27: we ship only to Mexico (`SHIP_COUNTRIES=MX`) yet quoted USD, making the sole possible buyer do FX in their head at the moment of decision. Why runtime and not a literal: **FX moves daily but `set-price` runs only when the USD price changes, and it rewrites USD patterns only** — a peso literal would be stale by the next morning and invisible to the price script. Fallbacks are correct rather than merely graceful: no `mxn` (cron not yet run against a new price), worker unreachable, or JS off all leave the static USD literal, which `set-price` does maintain. Today: $135.00 USD → **$2,399.00 MXN**. **Open:** the no-JS Spanish fallback still reads USD; fixing it would need a build-time fetch, which the static build deliberately has no server for |
| DEC-33 | How should The System section show that the Puks talk to the hub? | DECIDED | A **render of the whole system**: four Puks on an arc, each beaming its reading in to one central Tali, sitting right of the section copy (two-column head, stacking at 860px). Both bodies are the real Fusion models — `Tali v13.fbx` and `Puck v6.fbx` — rendered together in a single pass so lighting and perspective agree by construction. The **wireless signal stays SVG on top of the render**, so it uses `--accent`, animates, and can be re-aimed without re-rendering | `scripts/3d/hub_render.py`, `scripts/3d/textures.py`; `src/components/SystemLink.astro`, `System.astro`; `public/images/system-hub.webp`; `home.json` + both schemas (`systemLinkAlt`) | 2026-09-01: the section headline is "one hub for unlimited zones", and nothing on the page showed more than one zone. Two earlier attempts were rejected: a hand-drawn SVG Puk (no model existed yet, and it could never match a render), then a pair of real photographs — Puk in the rack, Tali on the counter — which were honest and needed no CGI, but could only ever depict a **single** zone, which is the opposite of the claim. **Arc centres come from the renderer, not from guesses**: `hub_render.py` prints each object's projected pixel position and the SVG anchors its waves to those, so the signal starts on the Puks themselves. Three traps cost real time and are guarded in the script's comments: `bounds()` read un-evaluated `matrix_world`, so parenting and constraints hadn't landed and the stale centre silently mis-aimed the camera; the camera is now aimed by an explicit rotation rather than a `TRACK_TO` constraint, which exists only on the evaluated copy; and rotating the hub to face the camera also rotated the e-paper UI plane, so the **camera moves, never the model**. The carbon weave broke twice for one reason — `TWILL_SCALE` was moved off the **0.42** that `explode_render.py` had already proven on these exact models, stretching a 1.5mm tow into stripes; the tile is also now filled tows with a cross-gradient, since outlining each cell collapsed into a grille at render scale. Blender 5 renamed the EEVEE engine again, so the script picks whichever enum the build offers. Output is WebP-with-alpha written by Blender (`sips` cannot), **47 KB** versus 896 KB as PNG, matching the exploded-view frames' format |
| DEC-34 | Where does a customer go for help after they own a Tali? | DECIDED | A **separate `/support/` + `/es/support/`** page — indexed and in the sitemap, not a landing-page section. It is **three panels shown one at a time** behind a sticky tab bar (Getting started · Troubleshooting · Contact us), so someone arriving with a problem picks a lane instead of scrolling past the sales page. **Troubleshooting is an index, not an accordion**: each entry is its own page at `/support/<slug>/` with a full markdown body, so an answer can run long and carry headings, an illustration or a video. Getting started is the same collection under `section: start`, rendered as a numbered walkthrough whose summaries read as steps and whose titles link to the full article. The contact form is the landing page's own form, rendered inline rather than in a modal | `src/content/support/{en,es}/*.md` (17 articles each), `src/content/site/support.json`; `src/components/Support.astro`, `SupportArticle.astro`, `ContactForm.astro` (extracted from `Contact.astro`); `src/pages/support/` + `es/support/`; `Header.astro`, `Footer.astro`, `Landing.astro`; both schemas | 2026-09-03: asked for as "a separate page, not infinite scrolling", with getting started / troubleshooting FAQ / contact. Two structural calls beyond the brief, both the author's: **tabs rather than a fourth long scroll** (the whole complaint was scrolling), and — after the first pass shipped accordions — **one page per issue** so articles can be written properly and illustrated. That second change is why the bodies are markdown and the two languages sit in **separate files** for the first time in this repo: a markdown file has one body. The slug is what pairs them (`en/pair-a-puk.md` ↔ `es/pair-a-puk.md`), and the language switcher falls back to the other language's index rather than 404 when a twin is missing. Keystatic edits them as two collections through `fields.markdoc({ extension: 'md' })` — **plain `.md`, not Keystatic's default `.mdoc`**, so Astro renders them with no new integration; verified by reading all 34 files back through `createReader`. Video is a **frontmatter field** (`video` + `image` as its poster), not a markdoc tag, for the same reason: a `{% video %}` tag would print as literal text in a plain-markdown build. Panels are hidden by CSS keyed on a `js` class set in `<head>`, so there is no flash of all three and **no-JS gets every section as one ordinary page** with the tabs behaving as anchors. `Contact.astro` was split so the form has one implementation in two presentations (modal on the landing, inline here); the captcha script now loads on a `contact-form:shown` event instead of the dialog's open handler. The header gained a Support link and absolute section hrefs off-landing (a bare `#system` pointed at nothing on `/support/`), and its Contact item targets the support page's own contact tab when you are already there. 2026-09-03 (later): three articles supplied by the author replaced or extended the drafts — `wifi-wont-connect` and `puk-wont-pair` were rewritten from his copy (keeping the `Síntoma:` structure as headings) and `factory-reset` is new. Two of his facts **contradicted the hedged guesses** in the getting-started walkthrough and were propagated: Tali sees **2.4 GHz only** (not "pick 2.4 if your router splits the bands"), and an early manufacturing batch has a screw-torque defect that stops the Puk's battery tab pulling free — `connect-wifi` and `pair-a-puk` now state both and link to the troubleshooting articles. The author wrote "2.5 GHz" throughout; written as **2.4 GHz**, since no such consumer band exists, and flagged to him. **Support addresses are per-locale page copy**, not site settings: `support@tali.my` on `/support/`, `soporte@tali.my` on `/es/support/`, announced in the contact panel and in each article's did-this-help block, and used for the form's mailto fallback. The form itself is unchanged — it still POSTs to the Formspree endpoint in `settings.json`, which lands in the same inbox as `hello@tali.my`; the footer and landing page keep announcing `hello@tali.my` site-wide. 2026-09-03 (later still): two **reference** articles added from author-supplied behaviour, deliberately separate from the troubleshooting ones that link to them — `alert-types` ("What kinds of alerts will I get?") and `identify-a-puk`. `alert-types` is where the alert model now lives: threshold alerts **arm only on a zone's first in-range reading** (so a zone that was never in range stays silent — the commonest reason a new install reports nothing), plus the three **system notifications** the app sends (mains power lost, cloud silent for >30 minutes, a Puk stopped reporting), plus **shake the Tali to silence an alert**. `identify-a-puk` covers the matching gesture: shake a Puk with the app's configuration section open and it animates on screen. Both gestures are the accelerometer that already ships for vibration sensing. The arming rule was written into `not-getting-alerts` and `zones-and-thresholds` first and then **cut back to a pointer in each** once the reference article existed — the trap stays where a stuck reader will hit it, the explanation lives in one place. It also invalidated the alert self-test this repo had invented ("narrow a limit below the current reading"), which would arm nothing and prove nothing; the test is now ordered in-range-first. `export-my-data` ("Can I download my Tali's data?") added from the author's answer: **not in the current pipeline, intended eventually, and until then support sends the file on request**. Written with no date and no roadmap promise, and it links to the contact panel rather than hardcoding an address, so the per-locale support email stays in one CMS field. Note this is the site's **first stated manual-support commitment** — every asker becomes a ticket. **WhatsApp is the second support channel** (`+52 81 1524 2085`), a button in the contact panel opening `wa.me`. The number is one site-wide field in `settings.json` written however it should read — the link strips it to digits — while the button label and the **"WhatsApp only, no calls or SMS"** caveat are per-locale support copy; the caveat is there because a phone number on a page reads as a phone line. The button uses `--accent`, not WhatsApp green, per the token rule. **`/hello`'s "Get help" now goes to `/support/#start`** instead of a mailto — someone standing over a new device wants the steps, not a blank email — and the walkthrough now closes with a contact block (`startHelp`) so that route still ends at a person. **Open:** the `wa.me/52…` form is untested; Mexican mobile numbers historically needed a `1` after the country code (`521…`) for WhatsApp, so open the link on a phone once before this ships. **Open:** the shake gestures' specifics are unverified — whether silencing snoozes or dismisses until re-trigger, whether it clears the app notification too, and how long the Puk animation runs. Whether the three system notifications need Cloud Pro is also unstated, so the article does not claim a tier for them. **Open:** the six setup steps are written from what the site already claims (BLE pairing, the Wi-Fi hand-off, the battery tab, ten Puks per Tali) and have **not** been checked against the real app flow — screen order and wording still need an author pass (the 2.4 GHz caveat was since answered, above). The reply-time promise reuses the landing page's existing "less than 48 hours" rather than inventing a second, tighter one; the return-shipping terms left open by DEC-31 are now stated as copy here (`returns-and-warranty`) and still need deciding. No article carries an illustration yet; `public/images/support/` is the upload directory Keystatic writes to |

<!--
Row conventions:
- IDs are permanent and never reused.
- When resolving: apply the change, remove the TODO(DEC-n) markers, set
  Status to DECIDED, replace the default with the real answer, and add a
  dated note, e.g. "2026-07-01: chose Cloudflare Pages over GH Pages (perf)".
- When changing an earlier decision: Status → REVISED, keep the old answer
  in Notes.
- New questions discovered mid-build get appended here immediately, even if
  answered on the spot (Status DECIDED) — the ledger is also the rationale
  record.
-->

## Future plans (not built, structure may anticipate them)

- Companion app pages (features, store links) — nav and content model can take a second page per locale without restructuring.
- Real product photography replacing the CSS device mock (DEC-19).
